# 二叉树与递归
## STEP 1
1. 
- 树的定义：有层次关系N（N>=0）个节点的有限集合，空树N=0，非空树：有且仅有一个根节点
- 二叉树：节点的度<=2的有序树（这个度只包括和子节点的关系，和父节点无关）（空树，只有根节点的树，只有左子数，只有右子树，左右子树均可
- 满二叉树：只有最后一层有子节点，不存在度为1的节点，高为h，节点个数为2^h-1的二叉树
- 定义一个二叉树：用结构体+指针来定义
```c
typedef struct treenode{
    int data;
    struct treenode* left;
    struct treenode* right;
}
```
2. 
- 二叉树的存储结构
  - 顺序存储：用数组存储，tree[0]处空缺，方便下标与数组中的序号相等，和完全二叉树一样，要求节点序号与满二叉树相同，只有最后两层才可以出现叶子节点（存储快，增删慢，且仅适用于完全二叉树）
  - 链式存储：用链表存储，链表节点对应树的节点，其中包含一个数据域和若干个指针域，增加指向父节点的指针就构成了三叉链表存储结构，含有n个节点的二叉链表中含有n+1个空链域（存取满，增删快）
- 二叉树的特点
  - 每个节点最多有两个子节点
  - 左右子树有序
  - 递归结构：二叉树由一个根节点和两棵互不相交的左右子树构成
  - 除根节点外，每个节点有且仅有一个父节点，节点数=边数+1
  - 具有层次关系
  - 遍历方式丰富，存储方式灵活
3. 
- .c文件中借助ai的部分比较多，但我也经过了认真的学习了，所以在这个.md文件中我尽可能把细节写的详细点，尽量不去借助ai，我觉得我学习完成之后可以做到了解树的基本概念，知道一个树的程序由哪五个函数组成，每个函数的作用是什么
- <mark>首先，要建立一棵树，我们需要创建两个结构体，其中一个存放数值域，和一个bool量去判断每一个节点是否是空节点，另外的一个结构体中存放的是节点数组，和数组的最大容量。这两个结构体，前者用来描述一个节点，而后者用来描述整棵树</mark>
- <mark>其次就是第一个函数void init_tree(SeqBiTree *tree)，它的主要作用是初始化整棵树，设定好二叉树的大小为MAX_TREE_SIZE，其次，利用for循环，逐个位置进行清空，然后data归零，用第一个结构体中的bool量，used，去排除所有没有节点的位置</mark>
- <mark>bool set_root(SeqBiTree *tree, int value)，这个函数的作用是用来固定根节点，将根节点的下标确定为1，并且确保数组可以放得下</mark>
- <mark>bool set_left_child(SeqBiTree *tree, int parent_node, int value)，第三四两个函数是用来帮助整棵树进行延伸的函数，分别确立其左孩子和右孩子，主要利用的是根节点为i，左节点为2i，右节点为2i+1，同时用if条件确保孩子节点一定有对应的父节点，且孩子节点的下标不会出现越界的情况，最后去给孩子节点赋值value</mark>
- <mark>void level_order(SeqBiTree *tree)，这个函数是设计的遍历函数，作用是按节点下标打印整棵树，节点为空则打印-1，且每打印一行换行，函数的第一步是找出整棵树的最后一个节点，从而可以知道打印到了多少层，是否为空树两个信息然后设置start=1，即可利用while循环开始遍历整棵树，并且通过遍历可以给所有的空节点在输出时变为负数，从而可以较为容易的分析每个节点的状态</mark>
## STEP 2
1. 创作数的过程写在了代码注释里
2. 打印一棵树需要用到函数递归去遍历整棵树
3. 为什么for循环无法实现：
   1. ***for循环只能走一条路，而树会分叉***，所以很难实现，走了左边就无法回头了
   2. ***for循环需要明确的指出下一个位置***，但是二叉树的非叶子节点，通常会对应两个孩子节点，因此没有明确的去向
## STEP 3
1. 
- 因为二叉树的遍历本身就是一次次递归的过程，递归的过程使得每一个节点都会被认作根节点一次，而"访问根"这个动作（printf）可以安排在处理左右子树之前、之间或之后——三个时机恰好对应"根左右 / 左根右 / 左右根"。递归负责"每个节点都被访问到"，printf 的位置负责"访问顺序"。所以仅需改变printf的位置就可以实现前序遍历，中序遍历和后序遍历三种情况。
- 1. 首先先调用preorder1，打印根节点1，然后因为二叉树是有序树所以它会直接进入左子树当中
  1. 其次，调用preorder2，打印左子树2，因为是前序遍历，所以继续进入2的左子树
  2. 调用preorder4，打印左子树4，因为4没有子节点，或者说它的子节点都为NULL，所以直接返回
  3. 接下来会回到2，打印右子树5，同理返回1
  4. 从1开始打印右子树3
  5. 打印3的左子树6
  6. 最后打印仅剩的3的右子树7
- 为什么最后两个值不需要采用指针的形式，而是可以直接使用：原因在于int depth这个函数构建出了一条很好的数据传递结构，我们所需要的max_depth返回值是通过return的形式逐层向上传递，而一般是需要不通过返回值，或者直接调取改变某个变量的时候才需要我们去用指针的形式，先通过地址的形式储存下来然后再进行远程
- 感悟：二者有类似的地方，就是都是进行重复的运算并且返回最终的值，一般他们都有初始状态和最终的一个终止条件，方便及时走出循环或者递归，区别在于循环的路径是较为单一的且不需要函数的辅助，而递归它给我的感觉是嵌套式的，比较复杂